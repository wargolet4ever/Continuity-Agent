# 评分差距表

对照竞赛「最佳 Agent」方向的要求：**理解自然语言 → 分解任务 → 编排模型与工具 → 评估中间结果并迭代修正 → 交付成片**。

状态定义：**已有** = 可运行且有测试覆盖；**部分实现** = 能跑但有明确缺口；**缺失** = 不存在。

| # | 评分维度 | 状态 | 现在是什么 | 差在哪 |
|---|---|---|---|---|
| 1 | 理解自然语言需求 | **已有** | CREATE 接受一句创意，AUDIT 接受一句指令；有 API 时走模型，无 API 时走确定性解析并如实标注 | 无 API 时的理解是关键词结构化，不是语义理解 |
| 2 | 任务分解 | **已有** | 一句创意 → 3—5 镜规划 → 逐镜 canon、约束、prompt、路由 | 拍点骨架是固定的五拍模板，不会因题材调整结构 |
| 3 | 编排模型与工具 | **部分实现** | 7 步 CREATE 链路中 3 步调模型、4 步本地规则，轨迹逐步标注执行方与耗时；另有一个受控 MiniMax Hailuo V1 Provider，实现提交／轮询／取回三段异步链路 | 公开部署只编排 1 个外部模型（文本 LLM）。视频 Provider 默认关闭，只过了 mock 与冷启动，**尚未完成任何一次真实生成** |
| 4 | 评估中间结果 | **已有** ★ | 四档决策 + 规则溯源 + 置信度 + 最小修复 + 五类证据标签；支持上传短视频并抽取 5 个有序帧检查跨帧形变 | 未配置多模态模型时，视频只完成抽帧和像素统计，语义判断仍来自人工描述；identity 轴无量化 grounding |
| 5 | 迭代修正 | **部分实现** | 修订 prompt、前后复审对比、API 临时错误自动重试 ≤2 并降级 | 内容重新生成仍需人在环，没有自动重跑闭环 |
| 6 | 可见的多 Agent 执行轨迹 | **已有** | 每步记录名称／执行方／来源／耗时／成败／重试；CREATE 7 步、AUDIT 4 步，界面默认展开 | 「四个 Agent」实质是一套调度上的四组能力，不是独立进程 |
| 7 | 交付成片 | **缺失** | 交付 Production Package（分镜、prompt、模型策略、canon 草案、因果审计、轨迹），状态停在 `AWAITING EXTERNAL GENERATION` | 提交的公开版本不生成视频。完成了真实视频 API 可行性探测，密钥、网络、端点和请求结构验证通过，但生成任务在提交阶段因账户套餐权限被拒绝，因此未产生视频，也未接入正式生成链路。仓库中的 Hailuo 适配器默认关闭，开启后也只做单镜头、人工确认费用的提交，不做整片交付 |
| 8 | 日志与可复现 | **已有** | 25 字段 CSV，含 `run_mode` `evidence_source` `duration_ms` `retries` `failure_reason` `model_source`；旧版自动迁移；公开 UI／API 默认不返回原始日志 | 服务端仍是单进程共享 CSV，不是账户级数据隔离 |
| 9 | 失败处理与转人工 | **已有** | 临时错误重试 ≤2 → 降级为本地规则 → 证据等级下调 → 失败原因入库；`HUMAN REVIEW` 为独立决策档 | — |
| 10 | 部署可用性 | **已有** | CPU 创空间，无 GPU 依赖；无 Key 时全功能降级运行；图片与最长 20 秒视频均可进入 AUDIT；ModelScope 线上 CREATE／图片 AUDIT 与产品包下载已实测 | 视频 AUDIT 新版仍需部署后的线上验收；真实多模态 API 仍未配 Key 实测 |

## 与「创意工具」方向的对照

| 维度 | 状态 | 说明 |
|---|---|---|
| 解决具体瓶颈 | **已有** | 循环／闪回／多时间线片子的跨镜头一致性——单帧看不出、只有跨镜比对才能发现的错误 |
| 角色／场景一致性 | **部分实现** | 有规则与锚点决策（chain vs canonical_lookup），但无 embedding grounding |
| 剧本到分镜 | **已有** | CREATE 全链路 |
| 批量生成 | **部分实现** | 多 Take 排序（≤12 张），但不批量生成 |
| Prompt 管理 | **已有** | 每镜正向／负向，随 canon 约束自动注入 |
| 工作流自动化 | **部分实现** | 审计即落日志、修订 prompt 自动生成；生成与剪辑仍在工具之外 |

## 最大的三个差距，按补齐成本排序

1. **交付成片（#7）** —— 阻塞在账户计费方案，不是工程问题。适配器本身已经写完并通过 mock 测试；差的是一次真实付费生成的证据。
2. **自动重跑闭环（#5）** —— 依赖 #7。生成与审查两端都在，但 `REGENERATE` 永远不会自动花钱重提，必须人在环确认；这是刻意的产品决定，不是未完成项。
3. **视频语义理解与 identity grounding（#4）** —— 视频抽帧链路已经补齐，但测试空间仍未给 AUDIT 配置多模态模型。当前会如实标注 `VIDEO FRAME + RULE CHECK`，不拿像素相似度冒充物体形变检测。

## 一句话自评

**评估能力是真的，而且比多数同类项目扎实；Agent 性从「四个界面标签」补到了「两条真实链路 + 可见轨迹 + 失败降级」；交付成片这一格仍然空着——原因是账户权限而非能力缺失，适配器已经写完并默认关闭。下一道证据门槛不是继续写接口，而是完成一次真实付费生成并把结果送回 AUDIT。**
