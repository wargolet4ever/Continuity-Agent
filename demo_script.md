# 60 秒演示脚本

目标：一分钟证明两件事——Agent 能把一句创意变成可生产方案，也能直接检查视频中单帧看不出的连续性错误。

建议录屏 1280×900。视频审查使用本地规则，不调用付费接口；不要点击“确认并提交视频任务”。

## 0:00—0:07　问题

**画面**：首屏标题。

**念白**：

> “循环和闪回的片子，同一个空间会被反复生成。最致命的错误，往往不是某一帧难看，而是物体沿时间轴突然变了。”

## 0:07—0:23　CREATE

**操作**：选择“一个潜水员在沉船里发现了自己的照片”→ 点“开始”→ 展示 7 步 Trace 和 Production Package 下载。

**念白**：

> “一句创意进去，它理解故事、规划镜头、装配 Canon、生成约束和 Prompt、选择模型档位，再审一遍因果链。理解与创作可以交给模型，一致性由确定性规则保证。”

## 0:23—0:39　真实视频 Demo A

**操作**：切到 AUDIT → 点“Shot 19 · 操纵台整体变形”→ 让示例视频和 5 张时间帧同时入镜 → 展示 `REGENERATE / LOC-B06`。

**念白**：

> “这是制作中真实失败的 Shot 19。Agent 自动从五秒视频抽取五个时间点：操纵台从一套结构变成了另一套结构。它命中跨帧几何稳定规则，必须整镜重生成。”

## 0:39—0:53　真实视频 Demo B

**操作**：点“Shot 21 · 拉杆变形 + 钥匙错位”→ 展示 5 帧和三条问题。

**念白**：

> “Shot 21 同时有三个问题：拉杆变形、钥匙插槽跑到错误位置、拉杆运动不符合既定机制。单看一帧可能合理，放回时间和 Canon 才知道第三幕的合作动机被破坏了。”

## 0:53—1:00　证据边界

**操作**：定格在 `VIDEO FRAME + RULE CHECK` 横幅。

**念白**：

> “它也会说明证据边界：现在抽帧是真实的，但未配置视觉模型时，语义错误来自人工描述和规则；接入多模态模型后才会升级为 VIDEO VISUAL AUDIT。”

## 录制检查

- 首次打开免费 CPU 空间，先等待 Running 并刷新一次，再开始录屏。
- 提前点一次两个视频 Demo，让解码依赖和媒体缓存完成冷启动。
- 画面中必须清楚出现：5 张按时间排序的关键帧、`LOC-B06`、`SHOT-21A`、`SHOT-21B`、证据来源标签。
- 不输入 API Key、访问码，不点击任何产生费用的按钮。
