# 60秒真实视频接口演示脚本

前提：在独立测试空间录制，`MiniMax-Hailuo-2.3-Fast`、768P、6秒；真实生成等待部分剪掉，不展示 API Key、访问码、账户、余额、完整 task_id 或下载 URL。

## 0:00—0:08｜一句话说明价值

**画面**：CREATE 首屏。

**念白**：

> “这个 Agent 不只是写 Prompt。它把一句故事拆成镜头、建立连续性规则，再把一个镜头交给真实视频模型。”

## 0:08—0:22｜CREATE 规划

**操作**：输入一句创意并运行 CREATE，滚到7步 Trace和分镜计划。

**念白**：

> “模型负责理解和创作；Canon、连续性约束、路由和因果检查由确定性规则执行。每个镜头都带着 Prompt、时长、画幅和锚点。”

## 0:22—0:36｜人工确认付费提交

**操作**：展开“受控视频生成”，选择 Shot 1，载入 Prompt，上传首帧。让预计费用 `¥1.35` 入镜；输入访问码时暂停或遮挡录屏，勾选确认并提交。

**念白**：

> “生成不是自动触发的。系统先检查首帧规格、并发、今日次数和预算，再要求人确认费用。只有通过这些门槛，才向 MiniMax 提交异步任务。”

## 0:36—0:44｜状态机

**画面**：显示 `SUBMITTED`，随后剪切到已生成完成的同一任务；点击“刷新生成状态”。

**念白**：

> “提交后保存任务状态，通过 task ID 轮询。查询遇到临时错误最多重试两次，但付费 POST 不盲目重发，避免重复扣费。”

## 0:44—0:53｜真实结果

**画面**：`SUCCEEDED` 和6秒生成视频播放。

**念白**：

> “任务完成后，Agent 用 file ID 取得短期下载地址，并立即在页面中返回生成结果。”

## 0:53—1:00｜送回审查

**操作**：切到 AUDIT，展示该镜头关键帧的证据等级和判断。

**念白**：

> “生成不是终点。结果还会回到连续性审查，形成规划、生成、检查和修复的闭环。”

## 录完后的关闭动作

1. 将测试空间 `ENABLE_VIDEO_GENERATION` 改回 `0`；
2. 删除测试空间中的 `MINIMAX_VIDEO_API_KEY` 和 `VIDEO_ACCESS_CODE` Secret；
3. 确认正式比赛空间从未启用视频功能；
4. 保存生成视频和费用截图，但截图不得包含 Key 或完整账户信息。
